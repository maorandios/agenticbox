import { RouteStub } from "@/components/shared/RouteStub";

export default function TasksPage() {
  return (
    <RouteStub
      title="משימות"
      body="רשימת המשימות תיבנה בשלב הבא. נתוני הדמה כבר זמינים ב־Mock Data."
    />
  );
}
